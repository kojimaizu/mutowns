<h1>おススメしたい音楽トーク♪</h1>

<% if flash[:success] %>
     <div class="alert alert-success" alert-dismissible fade show" rore="alert">
       <%= flash[:success] %><button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="閉じる"></button>
     </div>
<% end %>

<div class="mb-3 mt-3">
    <ul>
        <% @mutalks.each do |mutalk| %>
          <div>
          <%= link_to mutalk.user.name, user_path(mutalk.user), class: "text-decoration-none" %> <span class="text-muted">投稿時間 <%= mutalk.created_at %></span>
          </div>
          <p><%= link_to mutalk.title, mutalk %> <br> <%= mutalk.content %></p>
        <% end %>
    </ul>
</div>

<%== pagy_bootstrap_nav(@pagy) %>